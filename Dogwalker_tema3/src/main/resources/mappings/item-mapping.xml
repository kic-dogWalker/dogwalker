<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="ItemDAO">
	<!-- 아이템 목록 조회 메서드의 SQL 쿼리 -->
     <select id="getAllItems" resultType="com.admin.model.ItemVO">
    SELECT * FROM item;
	</select>

    <!-- 아이템 추가 메서드의 SQL 쿼리 -->
    <insert id="insertItem">
	    INSERT INTO item (it_num, it_name, it_img, it_content, it_price, it_rdate, it_cnt, it_deleted, catecode)
	    VALUES (#{it_num}, #{it_name}, #{it_img}, #{it_content}, #{it_price}, sysdate, #{it_cnt}, #{it_deleted}, #{catecode})
	</insert>

  </mapper>